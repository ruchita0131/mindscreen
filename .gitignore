# Environment files — NEVER commit these
.env
.env.local
.env.production
backend/.env
frontend/.env
# Allow frontend prod env (only contains public backend URL, no secrets)
!frontend/.env.production

# Python
__pycache__/
*.py[cod]
backend/venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/
frontend/dist/
frontend/node_modules/

# ML Models (too large for git — use Git LFS or store separately)
*.pt
*.pkl
*.bin
backend/ml/checkpoints/

# Docker
postgres_data/

# IDE
.vscode/
.idea/
*.swp
